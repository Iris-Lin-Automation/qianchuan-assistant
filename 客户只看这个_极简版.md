# 客户只看这个（极简版）

你只需要做三件事：

1. 双击 `一键安装依赖.bat`（首次安装）
2. 双击 `启动控制台.bat`（填 Webhook，点保存）
3. 每天自动跑，或手动双击 `影刀调用Python.bat`

---

## 你不需要做的事

- 不需要 `extension` 插件开发
- 不需要 `opdev` / npm / 上传版本
- 不需要配置飞书开放平台凭证

---

## 每天自动流程（真实交付）

1. 影刀定时登录抖店/千川并导出 Excel  
2. 保存为：`data/today.xlsx`  
3. 影刀最后一步调用：`影刀调用Python.bat`  
4. 系统自动计算并把日报卡片推到飞书群  

---

## 常见问题（1 分钟排查）

- 群里没消息：先看 Webhook 是否填对，再手动双击 `影刀调用Python.bat`
- 报“找不到 today.xlsx”：确认影刀导出路径是 `data/today.xlsx`
- 想看图表：打开 `data/dashboard.html`（本地可视化）

---

## 交付边界（给老板/客户）

本项目默认交付的是：**影刀 + Python + 飞书群卡片 + 本地看板**。  
插件开发链路属于可选高级能力，不是必须项。
